import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danielmatias.solucaocondominios',
  appName: 'Solução Condominios',
  webDir: 'dist'
};

export default config;
